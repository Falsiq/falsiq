# Evaluation smoke fixtures

These tasks and workspaces exist only to exercise the offline harness. They are
not benchmark candidates and must never be included in a development or
held-out corpus split.
